# Diagnostics Standard

Diagnostics must check the operating system, storage, configuration, port, health endpoint, database, disk space, and logs.

A support bundle may contain diagnostic output and recent logs, but must exclude raw configuration, passwords, database files, backups, uploads, and customer documents.
