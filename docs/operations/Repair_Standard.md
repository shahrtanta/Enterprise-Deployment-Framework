# Repair Standard

Safe automatic actions include creating missing runtime folders, restoring configuration only when missing, clearing stale temporary files, and generating a report.

Repair must never delete databases, backups, uploads, or overwrite valid runtime configuration.
