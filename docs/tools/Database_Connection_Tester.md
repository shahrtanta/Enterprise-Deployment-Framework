# Database Connection Tester Standard

- Build connections from structured fields.
- Use a bounded timeout.
- Open a real connection and execute a lightweight query.
- Return deterministic exit codes.
- Support silent installer use.
- Produce machine-readable results.
- Never expose passwords in process arguments or output.
