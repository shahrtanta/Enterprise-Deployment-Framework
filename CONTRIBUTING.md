# Contributing

- Keep rules testable and technology-aware.
- Never introduce hard-coded customer paths or credentials.
- Preserve user data by default.
- Every template must define validation and failure behavior.
- Document breaking changes in `CHANGELOG.md`.
